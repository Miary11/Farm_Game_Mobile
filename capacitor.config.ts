import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Farm_Game_Mobile',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
